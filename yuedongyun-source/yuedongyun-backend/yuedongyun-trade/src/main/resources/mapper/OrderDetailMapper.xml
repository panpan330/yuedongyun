<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuedongyun.trade.mapper.OrderDetailMapper">

    <select id="countEnrollNumOfTraining" resultType="com.yuedongyun.api.dto.IdAndNumDTO">
        SELECT training_id AS id, COUNT(training_id) AS num
        FROM order_detail
        ${ew.customSqlSegment}
        GROUP BY training_id
    </select>
    <select id="countEnrollTrainingOfMember" resultType="com.yuedongyun.api.dto.IdAndNumDTO" >
        SELECT user_id AS id, COUNT(training_id) AS num
        FROM order_detail
                 ${ew.customSqlSegment}
        GROUP BY user_id
    </select>
</mapper>

