<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuedongyun.campaign.mapper.UserVoucherMapper">

    <select id="queryMyVouchers" resultType="com.yuedongyun.campaign.domain.po.Voucher">
        SELECT c.id, c.discount_type, c.`specific`, c.discount_value, c.threshold_amount,
               c.max_discount_amount, uc.id AS creater
        FROM user_voucher uc
            INNER JOIN voucher c ON uc.voucher_id = c.id
        WHERE uc.user_id = #{userId} AND uc.status = 1
    </select>
    <select id="queryVoucherByUserVoucherIds" resultType="com.yuedongyun.campaign.domain.po.Voucher">
        SELECT c.id, c.discount_type, c.`specific`, c.discount_value, c.threshold_amount,
               c.max_discount_amount,  uc.id AS creater
        FROM user_voucher uc
            INNER JOIN voucher c on uc.voucher_id = c.id
        WHERE uc.id IN
        <foreach collection="userVoucherIds" separator="," item="id" open="(" close=")">
            #{id}
        </foreach>
        AND uc.status = #{status}
    </select>
</mapper>

