<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuedongyun.workout.mapper.WorkoutRecordMapper">

    <select id="countLearnedSessions" resultType="com.yuedongyun.api.dto.IdAndNumDTO">
        SELECT session_id AS id, COUNT(1) AS num
        FROM workout_record
        WHERE user_id = #{userId}
        AND finished = 1
        AND finish_time &gt; #{begin} AND finish_time &lt; #{end}
        GROUP BY session_id;
    </select>
</mapper>

